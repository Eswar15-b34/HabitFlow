# HabitFlow — Cloud Sync Edition

This version adds email/password Login + Register and Supabase cloud synchronization. Follow `SUPABASE_SETUP.md` before deploying.

The website files are static; Supabase provides authentication and the database.
